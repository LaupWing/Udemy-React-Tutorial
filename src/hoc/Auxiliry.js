const aux = (props) => props.children
// This is wrapper component which allows us to return adjecent elements
// Usually in react you can only return one parent element
// But through this little hack its possible to returnn multiple elements
export default aux